# Site title and subtitle. This is used in _includes/header.html
title: 'EDI Data Package Best Practices'
subtitle: 'Recommendations for data packages described with EML metadata'

# if you wish to integrate disqus on pages set your shortname here
disqus_shortname: ''

# if you use google analytics, add your tracking id here
google_analytics_id: ''

# Enable/show navigation. There are there options:
#   0 - always hide
#   1 - always show
#   2 - show only if posts are present
navigation: 1

# URL to source code, used in _includes/footer.html
codeurl: 'https://github.com/EDIorg/data-package-best-practices'


# Default categories (in order) to appear in the navigation
sections: [
    ['about', 'About'],
    ['tut', 'Information for Contributors'],
    ['ref', 'Reference'],
    ['dev', 'Developers'],
    ['post', 'Posts']
]

# Keep as an empty string if served up at the root. If served up at a specific
# path (e.g. on GitHub pages) leave off the trailing slash, e.g. /my-project
# baseurl: 'https://github.com/mobb/fork_test3_index_page2'
baseurl: '/data-package-best-practices'

# Dates are not included in permalinks
permalink: none

# Syntax highlighting
highlighter: rouge

# Since these are pages, it doesn't really matter
future: true

# Exclude non-site files
exclude: ['bin', 'README.md']

# Use the kramdown Markdown renderer
markdown: kramdown
redcarpet:
    extensions: [
        'no_intra_emphasis',
        'fenced_code_blocks',
        'autolink',
        'strikethrough',
        'superscript',
        'with_toc_data',
        'tables',
        'hardwrap'
    ]



# Section for adding collections
# collections:
#    guides:
#        output: true
#        permalink: /:collection/:name



theme: jekyll-theme-minimal
